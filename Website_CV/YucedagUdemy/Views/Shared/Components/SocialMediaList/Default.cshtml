@using EntityLayer.Concrete
@model List<SocialMedia>

<nav role="navigation">
    <ul class="nav justify-content-left">
        @foreach(var item in Model)
        {
            <li class="nav-item"><a class="nav-link" href="@item.Url" title="@item.Name"><i class="@item.IconUrl"></i><span class="menu-title sr-only">@item.Name</span></a></li>
        }
    </ul>
</nav>